#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kss/bin/kss-node" "$@"
  ret=$?
else 
  node  "$basedir/../kss/bin/kss-node" "$@"
  ret=$?
fi
exit $ret
