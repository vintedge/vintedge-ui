#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../styledocco/bin/styledocco" "$@"
  ret=$?
else 
  node  "$basedir/../styledocco/bin/styledocco" "$@"
  ret=$?
fi
exit $ret
